using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Controls : MonoBehaviour
{

	//TODO EVIL THING: 
	// if(SceneManager.CurrentScene == SceneManager.Scene.GAMEPLAY)
	// pick a handful of random non-controls keys every reload 
	// if the player presses one it reloads the level (and tells them exactly what happened and why)
	// call SceneLoader.ReloadLevel()


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
		


    }
}
